import { describe, it, expect } from 'vitest';

describe('dummy baseline test', () => {
    it('should correctly evaluate a mathematical expression', () => {
        expect(1 + 1).toBe(2);
        expect(2 * 3).toBe(6);
    });
});
